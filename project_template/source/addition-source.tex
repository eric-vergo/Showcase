% A stand-in for the paper or notes a ported project starts from. The addition
% chapter attaches the theorem below to `addition_right_identity`, recording this
% path and line range as the statement's original source.
\begin{theorem}\label{thm:addition-right-identity}
For every natural number $n$, adding zero on the right leaves it unchanged.
\end{theorem}
